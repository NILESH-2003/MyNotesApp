{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
     
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html" // Serve index.html for all routes (SPA)
    }
  ]
}
